extends ../layout.html

block content
	div.hero-unit.span5.thumbnail
		h2 #{entity.name}
			form(method='post',
				action='/entities/'+entity.id,
				class='button_to')
				input(type="hidden", name="_method", value="delete")
				input.btn.btn-danger(type='submit', value='delete')

		form(method='post', action='/entities/#{entity._id}')
			input(type="hidden", name="_method", value="put")

			include _form
